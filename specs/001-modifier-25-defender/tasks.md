---
description: "Task list for 001-modifier-25-defender (v1 MVP), generated by /speckit-tasks"
---

# Tasks: Modifier 25 Defender (v1 MVP)

**Input**: Design documents from `/specs/001-modifier-25-defender/`
**Prerequisites**: plan.md (loaded), spec.md (loaded, 47 ACs across 8 EPICs), research.md (loaded), data-model.md (loaded), contracts/analyze-endpoint.md (loaded), quickstart.md (loaded)

**Tests**: included. Constitution Testing Discipline (TD-1, TD-2) requires unit tests on every new agent, retrieval, and schema module in the same PR.

**Organization**: tasks grouped by user story to enable independent implementation and testing of each story. The v1 design's EPIC dependency graph (EPIC-001 -> EPIC-002 -> (EPIC-003 || EPIC-004) -> EPIC-005 -> EPIC-006 -> (EPIC-007 || EPIC-008)) maps to the user-story phasing below: User Story 1 (P1) requires EPICs 001 through 005 plus a minimal slice of 008; User Story 2 (P2) adds EPIC-006 and the remediation slice of 008; User Story 3 (P3) is EPIC-007 plus nightly eval.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: can run in parallel (different files, no dependencies)
- **[Story]**: which user story this task belongs to (US1, US2, US3); blank for setup/foundation/polish
- File paths are absolute within the project root.
- Each task references the spec acceptance criterion (AC-XXX-N) it satisfies, where applicable.

## Path Conventions

Web application layout per [plan.md](./plan.md): backend at `app/`, frontend at `ui/src/`, tests at `tests/`, eval harness at `eval/`, infra at `infra/`. All paths shown below assume project root `modifier-25-defender/`.

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: project initialization, dependency setup, basic toolchain.

- [ ] T001 Create `pyproject.toml` with `uv` configuration: dependencies (FastAPI, LangGraph, Pydantic v2, OpenAI, Qdrant client, rank_bm25, transformers, ragas, langfuse, pytest, mypy, ruff, black, coverage), strict mypy on `app/`, ruff with Google docstring rules, coverage exclusions for I/O glue.
- [ ] T002 [P] Create `infra/docker-compose.yaml` with services: `qdrant`, `postgres` (Langfuse backend + LangGraph checkpoints), `langfuse-server`, `langfuse-worker`, `backend` (placeholder, no Dockerfile yet), `ui` (placeholder).
- [ ] T003 [P] Create `infra/Dockerfile.backend` (Python 3.11-slim base, install via `uv sync`, run uvicorn).
- [ ] T004 [P] Create `infra/Dockerfile.ui` (Node 20 base, npm install, build, serve via Vite preview).
- [ ] T005 [P] Create `.env.example` with `OPENAI_API_KEY=`, `QDRANT_URL=http://qdrant:6333`, `POSTGRES_DSN=`, `LANGFUSE_HOST=`, `LANGFUSE_PUBLIC_KEY=`, `LANGFUSE_SECRET_KEY=`. Add `.env` to `.gitignore` (already done).
- [ ] T006 [P] Create `Makefile` with targets: `corpus`, `synthetic-data`, `eval`, `eval-retrieval`, `eval-defensibility`, `eval-adversarial`, `dev`, `replay`, `check-emdash`.
- [ ] T007 [P] Create `ui/package.json` with React 18, Tailwind, Vite, Vitest, TypeScript 5.
- [ ] T008 [P] Initialize Tailwind: `ui/tailwind.config.ts`, `ui/postcss.config.js`, `ui/src/index.css`.
- [ ] T009 [P] Configure CI in `.github/workflows/ci.yml`: jobs for lint (ruff), format (black --check), type (mypy), unit tests (pytest), em-dash gate (Q1 scope from R10), eval-gate (`make eval`).
- [ ] T010 [P] Configure nightly eval in `.github/workflows/nightly-eval.yml`: runs full eval against main without cache, posts delta report to Issues.
- [ ] T011 Implement `scripts/check_emdash.py` and `make check-emdash` target. Script greps changed files in scoped paths (R10) for U+2014; exit 1 on any match, with file:line output.

**Checkpoint**: scaffolding complete; CI runs but only the em-dash gate has anything real to enforce.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: shared infrastructure all user stories depend on. Schemas, LLM client wrapper, observability, DB session.

**CRITICAL**: no user story work can begin until this phase is complete.

- [ ] T012 [P] Create `app/schemas/text.py` with `TextSpan` and `Citation` Pydantic v2 models per [data-model.md](./data-model.md).
- [ ] T013 [P] Create `app/schemas/parser.py` with `ParsedEncounter`.
- [ ] T014 [P] Create `app/schemas/assessment.py` with `CriterionScore`, `CriteriaMap`, `DefensibilityAssessment`.
- [ ] T015 [P] Create `app/schemas/remediation.py` with `RemediationSuggestion`.
- [ ] T016 [P] Create `app/schemas/api.py` with `DefenderRequest` and `DefenderResponse` (compliance_status as `Literal["PASSED","BLOCKED"]` per Q2).
- [ ] T017 [P] Create `app/schemas/corpus.py` with `CorpusChunk`.
- [ ] T018 [P] Unit tests for all schemas under `tests/unit/schemas/test_*.py` (one per schema module). Tests cover: required-field validation, range validation (e.g., `confidence` 0..1), and the `TextSpan` invariant `text == source[start:end]` for happy paths. Constitution TD-1.
- [ ] T019 Create `app/llm/openai_client.py` with content-hash cache key composition (R7: prompt + retrieval_context_json + model_version + temperature + provider_tag). Implement local cache backed by `eval/.cache/`. Inject `temperature=0` default.
- [ ] T020 [P] Unit tests for `app/llm/openai_client.py` in `tests/unit/llm/test_openai_client.py`. Cover cache key determinism, temperature change forces re-eval, and stub LLM calls. Constitution TD-1.
- [ ] T021 Create `app/observability/langfuse_client.py` (singleton, reads env vars, exposes `start_trace`, `log_span`, `finalize_trace` returning `trace_id`).
- [ ] T022 [P] Unit tests for `app/observability/langfuse_client.py`. Stub the Langfuse SDK; verify trace_id propagation. Constitution TD-1.
- [ ] T023 Create `app/infra/db.py` with PostgreSQL connection helper for LangGraph checkpoints. Use SQLAlchemy or LangGraph's built-in Postgres saver.
- [ ] T024 Create `app/infra/settings.py` with Pydantic-settings model that loads env vars. Document required vs optional vars in module docstring.
- [ ] T025 Create `app/api/main.py` skeleton: FastAPI app, CORS middleware, healthz route. Wire to LangGraph compiled graph (graph itself comes later).
- [ ] T026 Create `data/synthetic/seed.txt` with the literal `42`. Constitution Principle IV traceability (Q4).
- [ ] T027 Create `data/procedure_codes_allowlist.json` with v1's six procedure codes (`11720`, `11721`, `11055`, `11056`, `11057`, `20600` plus a small holdout set). Constitution + AC-001-5.
- [ ] T028 Wire CI to fail on `data/synthetic/encounters/**` containing real-PHI markers (simple denylist of common real names, real DOB regex). New job in `.github/workflows/ci.yml`. Constitution Principle IV.

**Checkpoint**: foundation ready. All schemas defined, LLM cache wired, Langfuse instrumented, DB session reachable, em-dash and PHI gates active. User story implementation can now begin.

---

## Phase 3: User Story 1 - Coder reviews a Modifier 25 encounter for defensibility (Priority: P1) MVP

**Goal**: a coder pastes a synthetic encounter, gets a scored defensibility assessment with citations, in under 30 seconds, with the Compliance Guard verifying every claim. Spans EPICs 001 through 005 plus a minimal slice of 008.

**Independent Test**: from a fresh clone, run `make corpus && make synthetic-data && make dev`, paste a synthetic encounter into the UI, click Analyze, see a per-criterion verdict with at least one citation per criterion, and verify the response is `PASSED` (or `BLOCKED` with structured reasons).

### EPIC-001: Synthetic Encounter Generation

- [ ] T101 [P] [US1] Define the 12-dimension parameter space in `eval/synthetic/parameters.py`. Spec EPIC-001 4.2.
- [ ] T102 [US1] Implement template-based generator in `eval/synthetic/generator.py`. Deterministic with `seed=42`. AC-001-6.
- [ ] T103 [US1] Implement stratified sampler in `eval/synthetic/sampler.py` to produce 100 encounters with at least 25 PASS and 25 FAIL on overall verdict, covering at least 6 procedure codes. AC-001-1, AC-001-3, AC-001-4, AC-001-5.
- [ ] T104 [US1] Generate ground-truth labels (per-criterion + overall) during sampling and persist to `data/synthetic/labels.jsonl`. AC-001-2.
- [ ] T105 [US1] Implement 70/15/15 train/dev/test split with fixed assignment (deterministic by encounter_id hash). Persist split file. AC-001-8.
- [ ] T106 [P] [US1] Unit tests for the generator in `tests/unit/eval/test_generator.py`: determinism on seed=42, stratification yields the required PASS/FAIL counts, schema validity. Constitution TD-1.
- [ ] T107 [US1] Manual review pass: generate 20 encounters, write findings to `docs/synthetic-data-review-week1.md`, iterate templates if drift exceeds 10%. AC-001-7. (Manual task, marked complete when the review doc lands.)
- [ ] T108 [US1] Add `make synthetic-data` target wiring T102 through T105.

### EPIC-002: Reference Corpus and Retrieval

- [ ] T109 [P] [US1] Ingest source documents into `data/corpus/`: CMS NCCI Policy Manual chapters on E/M services, MLN Matters Mod 25 articles, AAPC public Mod 25 articles, JARALL Knowledge Center M25 posts, 2026 Medicare Claims Processing Manual relevant chapters. Target 30 to 60 documents. AC-002-1.
- [ ] T110 [US1] Implement chunker in `app/retrieval/chunker.py`: 200 to 400 token chunks with 50-token overlap. Stable `chunk_id` derivation (hash of source + offset). AC-002-6.
- [ ] T111 [US1] Implement metadata enricher in `app/retrieval/metadata.py`: source_document, section_heading, publication_date, authority_tier (CMS > AAPC > JARALL > OTHER). AC-002-4.
- [ ] T112 [P] [US1] Implement Qdrant client wrapper in `app/retrieval/qdrant.py`. Single-node Docker. Embeddings via `text-embedding-3-large` (3072 dim). AC-002-5.
- [ ] T113 [P] [US1] Implement BM25 index in `app/retrieval/bm25.py` over identical chunk set. AC-002-2.
- [ ] T114 [US1] Implement RRF fusion in `app/retrieval/fusion.py` (k=60). Inputs: ranked lists from dense and sparse retrievers.
- [ ] T115 [US1] Implement cross-encoder reranker wrapper in `app/retrieval/reranker.py` (`BAAI/bge-reranker-v2-m3`, local CPU inference). Top-20 in, top-5 out.
- [ ] T116 [US1] Implement orchestrating `Retriever` class in `app/retrieval/__init__.py` that composes chunker output, dense+sparse retrieval, RRF, rerank.
- [ ] T117 [P] [US1] Unit tests for chunker, metadata, RRF, reranker in `tests/unit/retrieval/test_*.py`. Constitution TD-1.
- [ ] T118 [US1] Build the 30-question retrieval eval set under `data/retrieval_eval/questions.jsonl`. Each question paired with the chunk(s) that should be retrieved. AC-002-3 eval methodology.
- [ ] T119 [US1] Implement `eval/retrieval/recall_at_5.py` harness producing `retrieval.recall_at_5` metric. AC-002-3.
- [ ] T120 [US1] Add `make corpus` target: ingest, chunk, enrich metadata, embed, persist Qdrant + BM25 indices. Idempotent. AC-002-6.

### EPIC-003: Documentation Parser Agent

- [ ] T121 [P] [US1] Author parser prompt at `prompts/parser/v1.md` with strict JSON-schema instruction and 3 worked examples (one PASS, one WEAK, one FAIL). Spec EPIC-003 6.1.
- [ ] T122 [US1] Implement parser agent at `app/agents/parser/agent.py`. Single GPT-4o call with `response_format=json_schema`. One-retry on schema failure (AC-003-4). Surfaces `ambiguous_segments` (AC-003-3).
- [ ] T123 [P] [US1] Unit tests for parser agent in `tests/unit/agents/parser/test_agent.py`. Cover schema validation, retry, ambiguous-segment surfacing, latency under p95 < 5s with stub LLM. Constitution TD-1, AC-003-5.
- [ ] T124 [US1] Implement parser eval harness at `eval/parser/field_accuracy.py` measuring AC-003-2 (>=90% field accuracy on dev split with 80%/120% offset coverage).

### EPIC-004: Defensibility Analyzer Agent

- [ ] T125 [P] [US1] Author 4 per-criterion analyzer prompts at `prompts/analyzer/{distinct_cc,separate_exam,independent_mdm,site_specificity}/v1.md`. Each includes the criterion's PASS/WEAK/FAIL definitions verbatim and instructs the LLM to cite at least one source span (AC-004-4).
- [ ] T126 [US1] Implement analyzer agent at `app/agents/analyzer/agent.py`. 4 GPT-4o calls (one per criterion) with retrieval context. Deterministic overall verdict computation (any FAIL -> overall FAIL; any WEAK with no FAIL -> overall WEAK; all PASS -> overall PASS). Spec EPIC-004 7.2.
- [ ] T127 [US1] Implement same-site special case: when E/M and procedure same site, `site_specificity` verdict computed without retrieval call, encoded as PASS with confidence=1.0 per data-model.md. Edge case from spec.
- [ ] T128 [P] [US1] Unit tests for analyzer agent in `tests/unit/agents/analyzer/test_agent.py`: per-criterion call structure, overall verdict computation, empty-evidence rejection, latency under p95 < 20s with stub LLM. Constitution TD-1, AC-004-1, AC-004-4, AC-004-6.
- [ ] T129 [US1] Implement defensibility eval harness at `eval/defensibility/accuracy.py` producing `analyzer.verdict_accuracy` and `analyzer.per_criterion_accuracy`. AC-004-2, AC-004-3.
- [ ] T130 [US1] Implement RAGAS faithfulness eval at `eval/faithfulness/ragas_runner.py` producing `analyzer.faithfulness`. AC-004-5.

### EPIC-005: Compliance Guard Agent (Constitution Principle II centerpiece)

- [ ] T131 [US1] Implement NLI inference wrapper at `app/agents/compliance_guard/nli.py` using `transformers` and DeBERTa-v3-large-mnli. Local CPU. <500ms per claim batch.
- [ ] T132 [US1] Implement Compliance Guard agent at `app/agents/compliance_guard/agent.py`. Verifies every Citation via NLI (AC-005-1). Threshold 0.75. Per Q2: any flagged claim demotes to BLOCKED (no DEGRADED in v1).
- [ ] T133 [US1] Implement structured `blocked_reasons` formatting per AC-005-4.
- [ ] T134 [P] [US1] Unit tests for Compliance Guard in `tests/unit/agents/compliance_guard/test_agent.py`: every-citation verification, BLOCKED state structure, latency under p95 < 3s with stub NLI. Constitution TD-1, AC-005-1, AC-005-4, AC-005-5, AC-005-6.
- [ ] T135 [US1] Build hand-crafted adversarial set of 20 hallucinated claims at `data/adversarial/claims.jsonl`. Each entry pairs a fake claim with the cited evidence that does not entail it.
- [ ] T136 [US1] Implement adversarial eval harness at `eval/adversarial/recall.py` producing `compliance_guard.adversarial_recall`. AC-005-2.
- [ ] T137 [US1] Implement false-positive eval at `eval/adversarial/false_positive.py` producing `compliance_guard.false_positive_rate` against the 30-question retrieval eval set's ground-truth-correct claims. AC-005-3.
- [ ] T138 [US1] Add an import-graph lint rule in `scripts/check_imports.py` enforcing that `app/agents/compliance_guard/**` has no import from `app/agents/parser/**`, `app/agents/analyzer/**`, `app/agents/drafter/**` (and vice versa for the synthesis agents importing the guard). Wire to CI. Constitution Principle II.

### EPIC-008 (minimal slice for US1): Demo UI

- [ ] T139 [P] [US1] Implement `ui/src/api/client.ts` typed POST /analyze client with full request/response types matching `app/schemas/api.py`.
- [ ] T140 [P] [US1] Implement encounter input panel in `ui/src/components/EncounterInput.tsx` (textarea + dropdowns for E/M code, procedure code, site).
- [ ] T141 [P] [US1] Implement criterion card in `ui/src/components/CriterionCard.tsx` with verdict, confidence, citations.
- [ ] T142 [P] [US1] Implement citation link with click-to-highlight in `ui/src/components/CitationLink.tsx`. AC-008-2.
- [ ] T143 [US1] Implement source-note panel with span highlighting in `ui/src/components/SourceNotePanel.tsx`.
- [ ] T144 [P] [US1] Implement BLOCKED banner in `ui/src/components/BlockedBanner.tsx`. Red styling, lists `blocked_reasons`. AC-008-3.
- [ ] T145 [US1] Implement Analyze page at `ui/src/pages/AnalyzePage.tsx` wiring components. Submit -> POST /analyze -> render PASSED with citations or BLOCKED with reasons. AC-008-1.
- [ ] T146 [P] [US1] Vitest unit tests on UI components in `ui/tests/components/test_*.tsx`.
- [ ] T147 [US1] Cross-browser sanity check on Chrome and Safari current versions (manual). AC-008-5.

### Wire it together

- [ ] T148 [US1] Implement Orchestrator at `app/agents/orchestrator/graph.py` using LangGraph. Nodes: Parser -> Analyzer -> ComplianceGuard. (Drafter added in US2.) State: `DefenderSession` typed model. Persist checkpoints to Postgres. Constitution Principle V.
- [ ] T149 [US1] Wire FastAPI route `POST /analyze` in `app/api/routes.py` to invoke the LangGraph compiled graph and return `DefenderResponse`. Per [contracts/analyze-endpoint.md](./contracts/analyze-endpoint.md). Include PHI denylist pre-check.
- [ ] T150 [US1] Integration test in `tests/integration/test_analyze_pass.py`: synthetic PASS encounter -> 200 PASSED with citations.
- [ ] T151 [US1] Integration test in `tests/integration/test_analyze_blocked.py`: synthetic encounter that triggers Compliance Guard -> 200 BLOCKED with `blocked_reasons`.

**Checkpoint**: User Story 1 fully functional. A coder can paste a synthetic encounter and get a scored, cited, guard-verified response. MVP shippable here.

---

## Phase 4: User Story 2 - Coder receives drafted remediation language (Priority: P2)

**Goal**: when overall verdict is WEAK or FAIL, the coder also sees targeted documentation language drafted for each weak criterion, with policy citations.

**Independent Test**: submit 20 synthetic encounters labeled WEAK or FAIL through the API; verify the response contains at least one `RemediationSuggestion` per weak criterion, each with at least one policy citation, and that a manual review finds at least 80% clinically reasonable.

### EPIC-006: Remediation Drafter Agent

- [ ] T201 [P] [US2] Author drafter prompt at `prompts/drafter/v1.md`. Single call with all weak criteria; instruct one-suggestion-per-criterion with at least one policy citation each (AC-006-2).
- [ ] T202 [US2] Implement drafter agent at `app/agents/drafter/agent.py`. Conditional invocation (only on WEAK or FAIL overall). Output: `list[RemediationSuggestion]`. Single GPT-4o call. Spec EPIC-006 9.1.
- [ ] T203 [US2] Wire drafter into the LangGraph in `app/agents/orchestrator/graph.py` via conditional edge: `analyzer -> drafter` when overall in {WEAK, FAIL}, else `analyzer -> compliance_guard`. Drafter still feeds compliance_guard.
- [ ] T204 [P] [US2] Unit tests for drafter in `tests/unit/agents/drafter/test_agent.py`: one-suggestion-per-weak-criterion (AC-006-1), every suggestion cites a policy (AC-006-2), no-edit invariant (no `note_text` in output, AC-006-3), latency under p95 < 10s with stub LLM (AC-006-5). Constitution TD-1.
- [ ] T205 [US2] Manual review of 20 dev-split suggestions for clinical reasonableness; record in `docs/remediation-review-week3.md`. AC-006-4.
- [ ] T206 [US2] Add remediation eval harness `eval/remediation/coverage.py`: assert at least one suggestion per weak criterion across the dev split.

### EPIC-008 (US2 slice): remediation suggestions UI

- [ ] T207 [P] [US2] Implement remediation panel in `ui/src/components/RemediationPanel.tsx`. Renders suggestions in a panel visually distinct from the source note. Marked clearly as suggestions. US2 acceptance scenario 3.
- [ ] T208 [US2] Wire `RemediationPanel` into `AnalyzePage`: only rendered when `remediations.length > 0`.
- [ ] T209 [P] [US2] Vitest tests on `RemediationPanel` and integration test on `AnalyzePage` rendering remediations.

**Checkpoint**: User Stories 1 and 2 work independently. Coders see scored verdict with optional remediation drafts.

---

## Phase 5: User Story 3 - Eval engineer enforces accuracy and safety in CI (Priority: P3)

**Goal**: any PR that regresses retrieval recall, verdict accuracy, per-criterion accuracy, compliance guard adversarial recall, or RAGAS faithfulness below the configured thresholds is blocked from merge.

**Independent Test**: open a PR that intentionally weakens any gate (lower retrieval k, weaker compliance threshold, etc.) and verify CI fails with a clear gate-failure message.

### EPIC-007: CI/CD and Quality Gates

- [ ] T301 [US3] Create `eval/thresholds.yaml` capturing AC-002-3 (recall@5 >= 0.85), AC-004-2 (verdict accuracy >= 0.85), AC-004-3 (per-criterion >= 0.80), AC-005-2 (adversarial recall = 1.00), AC-005-3 (false positive <= 0.10), AC-004-5 (RAGAS faithfulness >= 0.88), and the latency budgets (AC-002-5, AC-003-5, AC-004-6, AC-005-5, AC-006-5).
- [ ] T302 [US3] Implement eval orchestrator at `eval/run.py` that invokes all sub-harnesses (retrieval, parser, analyzer, compliance, faithfulness, latency) and emits `eval/reports/<timestamp>.json` matching the schema in [quickstart.md](./quickstart.md).
- [ ] T303 [US3] Implement gate checker at `eval/check_gates.py` that loads thresholds and the latest report and exits non-zero on any threshold violation, with file/line-style output naming the failing gate.
- [ ] T304 [US3] Wire `eval/check_gates.py` into `.github/workflows/ci.yml` as a PR-blocking job. AC-007-6.
- [ ] T305 [US3] Wire `eval/run.py` (no-cache mode) into `.github/workflows/nightly-eval.yml`. Post the report and any threshold-violation summary as a GitHub Issue or comment. R9.
- [ ] T306 [US3] Add reviewer-required protection on threshold changes: a CI job that detects when `eval/thresholds.yaml` changed and requires the PR description to contain "Threshold change rationale:" plus at least 50 characters; fail otherwise. US3 acceptance scenario 3.
- [ ] T307 [P] [US3] Unit tests for the gate checker at `tests/unit/eval/test_check_gates.py`: pass on a green report, fail on each individual threshold violation. Constitution TD-1.

**Checkpoint**: All three user stories independently functional. Quality gates enforced in CI.

---

## Phase 6: Polish and Cross-Cutting Concerns

**Purpose**: documentation, performance tightening, demo readiness.

- [ ] T401 [P] Write `docs/architecture.md`: full agent specs, state schemas, retrieval design (cite spec section 2.x).
- [ ] T402 [P] Write `docs/eval-methodology.md`: how each metric is computed, what the thresholds mean, how to interpret a report.
- [ ] T403 [P] Write `docs/synthetic-data.md`: 12-dimension parameter space, ground-truth labeling, regeneration command.
- [ ] T404 [P] Write `docs/limitations.md`: explicit list mirroring spec Out of Scope and Open Questions.
- [ ] T405 Code cleanup pass: remove TODOs, ensure all public symbols have Google docstrings (Constitution CS-2). Run `ruff check --fix` and `black .`.
- [ ] T406 Run `make eval` end-to-end on `main` and capture the report. Verify all gates green. Commit the resulting report at `eval/reports/baseline.json`.
- [ ] T407 [P] Build the `/quality` route in the UI (`ui/src/pages/QualityPage.tsx`) showing the most recent eval report. AC-008-4 alignment.
- [ ] T408 Run `quickstart.md` from a fresh clone on the maintainer's machine; record any deviation as a quickstart fix PR. Constitution + DoD reproducibility.
- [ ] T409 Record a 5-minute Loom-style demo video walking through paste, analyze, citation click, BLOCKED scenario, quality page. DoD line.
- [ ] T410 Verify no real PHI in any commit including deleted commits via `git log --all -p | scripts/check_phi.py`. Constitution Principle IV.
- [ ] T411 Update root `README.md` (parent dir) "Eval results" table with the baseline numbers from T406, replacing _TBD_ with actuals.

---

## Dependencies and Execution Order

### Phase Dependencies

- **Phase 1 (Setup)**: no dependencies, start immediately.
- **Phase 2 (Foundational)**: depends on Phase 1. BLOCKS all user stories.
- **Phase 3 (US1)**: depends on Phase 2. Internally respects EPIC dependency graph: 001 -> 002 -> (003 || 004) -> 005 -> 008-minimal.
- **Phase 4 (US2)**: depends on Phase 3 (specifically T148 orchestrator graph for the conditional edge to drafter).
- **Phase 5 (US3)**: depends on the eval harnesses produced inside Phase 3 (T119, T124, T129, T130, T136, T137). Can run in parallel with Phase 4 on a multi-developer team.
- **Phase 6 (Polish)**: depends on Phases 3 through 5.

### Within Phase 3 (US1)

- EPIC-001 (T101..T108) before EPIC-002 dependencies on synthetic data: actually corpus is independent of synthetic; the two can be parallel.
- EPIC-002 (T109..T120) and EPIC-001 (T101..T108) can run in parallel.
- EPIC-003 (T121..T124) and EPIC-004 (T125..T130) depend on EPIC-002 (retrieval) for analyzer. Parser does not need retrieval; can start as soon as schemas exist.
- EPIC-005 (T131..T138) depends on outputs of EPIC-004 (citations to verify).
- EPIC-008 minimal (T139..T147) depends on the schema layer; can build in parallel with the agents and integrate via T148 and T149.

### Parallel Opportunities

Within Phase 1: T002 through T011 are all `[P]` and can be done in parallel.

Within Phase 2: T012 through T018 are schema modules and tests, all `[P]`. T019, T021, T023, T024, T025 sequence after schemas land but T020 and T022 (their tests) are `[P]` once their target module exists.

Within Phase 3 (after T108 corpus indexed):
```text
EPIC-003 thread: T121 -> T122 -> T123 (P with T124)
EPIC-004 thread: T125 -> T126 -> T127 -> T128 (P with T129) -> T130
EPIC-005 thread: blocks on EPIC-004 outputs; T131 -> T132 -> T133 -> T134 (P with T135) -> T136 -> T137 -> T138
EPIC-008 thread: T139..T146 mostly P after schemas; T147 manual at end
```

---

## Implementation Strategy

### MVP First (User Story 1 only)

1. Phase 1 setup (~1 day).
2. Phase 2 foundational (~1 day).
3. Phase 3 US1 (~10 to 12 days for solo).
4. STOP and validate: run `make eval`, paste a synthetic encounter through the UI, verify a green report.
5. Demo-ready at this point. EPIC-006 and EPIC-007 are quality-of-life and quality-gate add-ons.

### Incremental Delivery After MVP

1. Phase 4 US2 adds remediation drafts (~3 days).
2. Phase 5 US3 adds CI gate enforcement (~2 days). Can run in parallel with US2 on staffed teams.
3. Phase 6 polish (~2 to 3 days).

### Cut Order (per spec R5, resolved Q3 option C)

If schedule slips:
1. Cut Phase 6 polish (skip Loom recording, ship a more minimal `/quality` page).
2. Cut Phase 4 US2 (Remediation Drafter); ship verdict-only demo.
3. Defer corpus expansion to v1.1.

NEVER touch Phase 5 US3 (EPIC-007 quality gates) or EPIC-002 retrieval or EPIC-005 Compliance Guard. These are Constitution Principle II and architectural-differentiator territory.

---

## Notes

- `[P]` tasks operate on different files with no dependencies.
- `[USx]` labels map task to a specific user story for traceability.
- Each user story is independently completable and testable per its Independent Test bullet.
- Constitution TD-1 requires unit tests in the same PR as new agent/retrieval/schema modules.
- Constitution Principle II requires `app/agents/compliance_guard/` to have no synthesis-side imports; verified by T138.
- Em-dash CI gate (Q1: option C scope) runs on all curated text in scoped paths.
- Commit after each task or logical group of `[P]` tasks. Branch is `feature/001-modifier-25-defender`.
- Verify each Phase's checkpoint independently before moving to the next.
